global:
  scrape_interval: 1s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/recording-rules.yml

scrape_configs:
  - job_name: pg-cdc-lab
    static_configs:
      - targets: ["host.docker.internal:9464"]

